version: '3.8'
services:
  app:
    build: .
    restart: unless-stopped

    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - /home/env/.env.blog-backend-go.dev
    ports:
      - "8080:8080"
    # volumes: 如需其他挂载可在此添加
